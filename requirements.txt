flask>=3.0
httpx[http2]>=0.27      # async HTTP client + HTTP/2 support
h2>=4.0                 # HTTP/2 backend for httpx
gunicorn>=22.0
gevent>=24.0            # async worker for gunicorn (non-blocking I/O)
